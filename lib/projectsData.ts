// lib/projectsData.ts
import { projectsData } from './data'; // Import from the original data file

// If you need to transform or process the data, do it here

export default projectsData; // Default export for easier import